import React from "react";
import { View, Text } from "react-native";
import tw from "twrnc";

const Blogs = () => {
  return (
    <View style={tw`bg-red-800`}>
      <Text>This is a Blogs Screen</Text>
    </View>
  );
};

export default Blogs;

